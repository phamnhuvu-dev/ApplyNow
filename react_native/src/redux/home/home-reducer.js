import HomeState from "./home-state";
import {ACTION_FETCH_POST} from "./home-actions";

export default (state, action, root) => {
  switch (action.type) {
    case ACTION_FETCH_POST:
      break;

    default:
      return state
  }
}